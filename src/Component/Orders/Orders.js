import React from 'react';
import { Container } from 'react-bootstrap';

const Orders = () => {
    return (
        <Container>
            <p>this is orders</p>
        </Container>
    );
};

export default Orders;